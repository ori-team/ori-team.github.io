---
// Um card de projeto. O layout (grade ou linha) vem do container via CSS:
// `.projects[data-view="grid"]` vs `.projects[data-view="list"]`.
import { Code, Globe, Terminal } from "../lib/icons";
import { siteUrl } from "../lib/url";
import RepoLink from "./RepoLink.astro";
import type { ProjectMeta } from "../data/projects";

const ICONS = { code: Code, terminal: Terminal, globe: Globe } as const;

interface Props {
  project: ProjectMeta;
}

const { project } = Astro.props;
const Icon = ICONS[project.icon];
---

<article class="project-card">
  <div class="project-icon" aria-hidden="true">
    <Icon width={28} height={28} />
  </div>
  <div class="project-body">
    <h3 class="project-name">
      <a href={siteUrl(`/projetos/${project.slug}/`)}>{project.name}</a>
    </h3>
    <p class="project-summary">{project.summary}</p>
    <ul class="project-tags" aria-label="Tecnologias">
      {project.tags.map((tag) => <li>{tag}</li>)}
    </ul>
    <RepoLink href={project.repo} />
  </div>
</article>

<style>
  .project-card {
    display: flex;
    gap: 1rem;
    padding: 1.25rem;
    border: 1px solid var(--sl-color-hairline-light);
    border-radius: 0.75rem;
    background: var(--sl-color-white);
  }
  .project-card:hover {
    border-color: var(--sl-color-hairline-shade);
  }
  .project-icon {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 3rem;
    height: 3rem;
    border-radius: 0.625rem;
    background: var(--sl-color-accent-low);
    color: var(--sl-color-text-accent);
  }
  .projects[data-view="list"] .project-card {
    align-items: center;
  }
  .project-body {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    min-width: 0;
  }
  .project-name {
    margin: 0;
    font-size: 1.05rem;
    letter-spacing: -0.01em;
  }
  .project-name a {
    color: inherit;
    text-decoration: none;
  }
  .project-name a:hover {
    color: var(--sl-color-text-accent);
  }
  .project-summary {
    margin: 0;
    font-size: 0.92rem;
    line-height: 1.55;
    color: var(--sl-color-gray-2);
  }
  .project-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem;
    margin: 0.15rem 0 0.35rem;
    padding: 0;
    list-style: none;
  }
  .project-tags li {
    font-size: 0.72rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    padding: 0.15rem 0.5rem;
    border-radius: 0.375rem;
    background: var(--sl-color-gray-6);
    color: var(--sl-color-gray-2);
  }
</style>
